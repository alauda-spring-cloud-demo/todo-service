spring:
  application:
      name: todo-service
  cloud:
    config:
      name: ${spring.application.name}
      fail-fast: true
      retry:
        max-attempts: 10000
        max-interval: 1000
logging:
  level:
    org.springframework.security: debug